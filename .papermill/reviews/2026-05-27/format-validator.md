# Format Validator Report (2026-05-27)

## Scope

Build verification, label resolution, venue formatting fit,
typesetting hygiene. Focus on the page-count drift since 2026-05-23
(14 to 17 pages) and any new format issues introduced by the 2026-05-27
revision pass.

## Build status

`make paper` succeeds. `main.pdf` is produced with 17 pages at
letterpaper 11pt.

- BibTeX: clean (`main.blg` shows no warnings).
- pdflatex: clean except for one `Overfull \hbox` warning (0.15 pt,
  cosmetic) and three hyperref unicode warnings (cosmetic; affect
  bookmarks).
- All `\cref` and `\Cref` references resolve.
- All `\cite` references resolve.

## Critical findings

None affecting build.

## Major findings

### FV-M1 (carry-forward, escalated). Page count is now 17 vs ~12 conference target

**Location**: Whole document.

**Problem**: Same as 2026-05-23 FV-M1, escalated. The 2026-05-23 build
was 14 pages and was flagged as 2 over the ~12 page target. The
current build is 17 pages, 5 over the ~12 page target. For the venues
named in the prompt:

- **Nature Methods**: format is short main + extensive supplementary.
  Current monolithic format does not fit; restructuring to extract
  validation into supplementary would shorten main text substantially.
- **Genome Biology**: no hard page limit. 17 pages is acceptable.
- **RECOMB**: 12-page LIPIcs limit. The article 11pt format compresses
  by 20 to 30% under LIPIcs typesetting; estimate the LIPIcs version
  at 13 to 14 pages. Would need additional 1 to 2 page trim under
  LIPIcs.
- **AISTATS**: 8-page main + unlimited appendix. Substantive
  restructure needed.

State.md commits to "Genome Biology primary, RECOMB conference
fallback". Under that commitment, the page count is fine for the
primary and trims needed for the fallback. The user prompt names
four primary targets, which conflicts with state.md.

**Suggestion**: Lock venue choice before further drafting. If Genome
Biology, length is fine and this finding is closed. If RECOMB,
substantive trim needed (see prose-auditor P-M2 for the
discussion-section restated paragraph as the easiest 0.5 page).

### FV-M2 (carry-forward). Overfull horizontal box in translation table is largely resolved; one residual cosmetic 0.15 pt overfull in introduction

**Location**: 
- `sections/introduction.tex` lines 22 to 32 (0.15 pt; cosmetic only).
- The 2026-05-23 finding of a 25.2 pt overfull in `translation.tex`
  appears resolved; the current build log shows no overfull entry for
  translation.tex.

**Comment**: The translation table fix is in. The 0.15 pt overfull in
the introduction is below the threshold most readers would notice
(LaTeX flags anything > 0 pt). Not blocking.

### FV-M3 (carry-forward). Three hyperref "Token not allowed in PDF string (Unicode)" warnings persist

**Location**: `main.log`.

**Problem**: Same as 2026-05-23 FV-M3. Cosmetic issue: caption math
that hyperref uses for bookmark text generates the warning. The
warnings do not block build but result in less-clean PDF bookmarks.

**Suggestion**: Wrap inline math in captions with
`\texorpdfstring{math}{plain}`. A 5-minute fix.

## Minor findings

### FV-min1 (carry-forward). PDF metadata fields are empty

**Location**: `pdfinfo main.pdf` shows blank Title, Author, Subject,
Keywords.

**Comment**: Same as 2026-05-23 FV-min1. Adding `\hypersetup{pdftitle
= {...}, pdfauthor = {Alexander Towell}}` to `main.tex` after
hyperref load would fix this. Submission portals at journals sometimes
auto-populate from PDF metadata.

### FV-min2 (new). Two figures (rank_diagnostic.pdf, marker_bias.pdf, visium_spectrum.pdf) are present in figures/ and resolve correctly in the build

**Comment**: Verified the build references and includes all three
figures correctly. Captions present.

### FV-min3 (new). The new RCTD subsection uses `\subsection{Worked example: RCTD as a C1--C2--C3 specialization}` which contains an em-dash-like double-dash

**Location**: `sections/translation.tex` line 65.

**Comment**: This is the LaTeX `--` en-dash convention (rendered as
"to" in printed PDF), not a Unicode em-dash. LaTeX-correct. No
violation of the no-em-dash convention. The same `--` appears in
`sections/identifiability.tex` line 26 ("$P$ has full column rank
$K$"). Verified all `\section` and `\subsection` titles for any
genuine em-dash use; none present.

## Reproducibility check

- `make paper` and `make sim` documented and verified to run.
- `scripts/run.R` (sim, seed 20260517), `scripts/visium.R` (real
  data), `scripts/rctd_compare.R`, `scripts/joint_mu.R`, and
  `scripts/sim.R` all present and listed.
- `data/` directory present; `data/download_data.sh` present for the
  Visium dataset.
- Figures (rank_diagnostic.pdf, marker_bias.pdf, visium_spectrum.pdf)
  generated by `scripts/figures.R` and included in `figures/`.
- Reproducibility is high. The visium.R seed is not stated in the
  validation text (2026-05-23 M-min3 carry-forward).

## Cross-verification status

- 2026-05-23 FV-M1 (page count): unchanged in kind, escalated in
  magnitude.
- 2026-05-23 FV-M2 (overfull boxes): largely resolved (the 25.2 pt
  translation table overfull is fixed; 0.15 pt intro residual is
  cosmetic).
- 2026-05-23 FV-M3 (hyperref unicode warnings): unchanged.
- 2026-05-23 FV-min1 (PDF metadata): unchanged.

## Severity calibration

No critical or major findings affecting submission build integrity.
FV-M1 (page count) is the only material issue and is venue-dependent.
The build is clean; the format is publication-ready under the Genome
Biology length convention.
